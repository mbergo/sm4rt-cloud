interface:
  display_name: "Floci Cloud"
  short_description: "Repo-specific patterns and workflows for floci-cloud"
  default_prompt: "Use the floci-cloud repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true